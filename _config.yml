title: Prompt Library
theme: minima
markdown: kramdown
kramdown:
  input: GFM
permalink: /:path/
exclude:
  - node_modules
  - vendor
  - .github
  - .ops
  - Gemfile*
  - package*.json
titles_from_headings:
  enabled:     true
  strip_title: true       # don’t duplicate the H1 in the content
  collections: true

# Defaults so pages render even if authors forget a layout
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: page
